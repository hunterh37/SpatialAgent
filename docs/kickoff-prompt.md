# Kickoff prompts

## New session — start Phase A

    Read PRD.md, spec/06-avatar.md, spec/07-memory.md, and docs/development-plan.md.
    Implement Phase A1 (BirdRig, BirdProportions, BirdPalette) in
    packages/Sources/CharacterKit/Bird/ with BirdRigTests per the plan.
    Stop when `make test` is green. No other phase.

## Continue — next step each session

    Read docs/development-plan.md and `git log --oneline -5`.
    Implement the next unstarted step in order; one step per session.
    Write its tests as the plan names them, keep `make test` green,
    then append a one-line status to docs/development-plan.md under that step.
